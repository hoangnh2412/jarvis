/**
 * Mutation dùng `ActionProps` kiểu jQuery ajax:
 * `before` / `onSubmit` / `success` / `error` / `complete`.
 *
 * @see {@link ActionProps} trong `lib/handleAction`
 */

export type { ActionProps } from '../../../lib/handleAction'
