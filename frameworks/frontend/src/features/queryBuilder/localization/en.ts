import type { QueryBuilderMessages } from './vi'

export const enMessages: QueryBuilderMessages = {
  title: 'Query Builder',
  description: 'Build filter conditions and emit AST on params.filter',
  addRule: 'Add rule',
  addGroup: 'Add group',
  loadingFields: 'Loading fields…',
  noFields: 'No fields available',
  reset: 'Clear filters',
}
